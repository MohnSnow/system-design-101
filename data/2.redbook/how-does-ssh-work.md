# 🔐 SSH是怎么工作的？三层架构详解

> 远程连接服务器的安全协议

![SSH工作原理](how-does-ssh-work.png)

SSH（安全外壳协议）用于在不安全网络上安全连接远程机器，三层架构 👇

📌 **传输层** — 提供加密、完整性和数据保护
📌 **认证层** — 验证客户端身份，确保只有授权用户能访问
📌 **连接层** — 将加密认证的通信复用为多个逻辑通道

💡 SSH有两个版本，SSH-2是IETF标准化的版本。日常开发中ssh命令用的就是SSH-2。

---

#SSH #安全 #Linux #运维 #程序员 #技术干货
